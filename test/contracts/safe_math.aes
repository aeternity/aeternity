
contract SafeMath =

  entrypoint add(x, y) = x + y
  entrypoint sub(x, y) = x - y
  entrypoint mul(x, y) = x * y
  entrypoint div(x, y) = x / y
  entrypoint rem(x, y) = x mod y
  entrypoint pow(x, y) = x ^ y

