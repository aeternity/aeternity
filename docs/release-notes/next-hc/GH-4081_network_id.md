* Removes the default consensus for networks with `network_id` starting with
  `hc_`
* Changes the key that defines the consensus in the config: it used to be
  called `name` and now it is renamed to `type`
