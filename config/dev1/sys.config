[

  { epoch, [
  ]},

  { aehttp, [
      {swagger_port_external, 3013}
  ]},

  {lager, [
      {handlers, [
          {lager_file_backend,
            [{file, "log/epoch.log"}, {level, info}, {size, 41943040}, {date, "$D0"}, {count, 10}]}
      ]}
  ]}
].
