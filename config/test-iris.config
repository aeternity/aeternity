[
  {kernel,
   [
    {inet_dist_use_interface, {127,0,0,1}},
    {logger,
     [ {handler, aeutils, aeu_lager_logger_handler, #{}}
     , {handler, app_ctrl, app_ctrl_bootstrap, #{}} ]}
   ]
  },

  {setup,
   [
    {data_dir, "../../rel/aeternity/data"}
   ]
  },

  {aecore,
   [
    {persist, false},
    {network_id, <<"local_iris_testnet">>}
   ]
  }
].
