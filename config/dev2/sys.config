[
  { epoch, [
  ]},

  { aehttp, [
      {swagger_port_external, 3023}
  ]},
  {aecore, [
      {password, <<"secret">>}
    ]
  },

  {lager, [
      {handlers, [
          {lager_file_backend,
            [{file, "log/epoch.log"}, {level, debug}, {size, 41943040}, {date, "$D0"}, {count, 10}]}
      ]}
  ]}
].
