-sname aeternity@localhost

-setcookie aeternity_cookie
-pa patches/ebin

# This setting should not be changed, since this will not do any harm if the
# system doesn't support kernel polling.
+K true

# Sets the number of scheduler threads to create and scheduler threads to set
# online based on the percentage of all available CPU cores.
# Should only be increased if your system supports more parallelism in
# the form of available CPU cores.
+SP 25
