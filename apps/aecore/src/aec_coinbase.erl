%%%-------------------------------------------------------------------
%%% @copyright (C) 2018, Aeternity Anstalt
%%% @doc
%%%     Module generated by aec_coinbase_gen
%%%     Initial supply of tokens: 276450333499323152460728285
%%% @end
%%%-------------------------------------------------------------------

-module(aec_coinbase).
-export([coinbase_at_height/1]).

-define(MULTIPLIER, 1000000000000000000).

-spec coinbase_at_height(non_neg_integer()) -> non_neg_integer().

coinbase_at_height(X) when not is_integer(X) orelse X < 0 ->
    error({bad_height, X});
coinbase_at_height(H) when H < 1 -> 0 * ?MULTIPLIER;
coinbase_at_height(H) when H < 4 -> 1 * ?MULTIPLIER;
coinbase_at_height(H) when H < 6 -> 2 * ?MULTIPLIER;
coinbase_at_height(H) when H < 8 -> 3 * ?MULTIPLIER;
coinbase_at_height(H) when H < 10 -> 4 * ?MULTIPLIER;
coinbase_at_height(H) when H < 12 -> 5 * ?MULTIPLIER;
coinbase_at_height(H) when H < 14 -> 6 * ?MULTIPLIER;
coinbase_at_height(H) when H < 16 -> 7 * ?MULTIPLIER;
coinbase_at_height(H) when H < 18 -> 8 * ?MULTIPLIER;
coinbase_at_height(H) when H < 20 -> 9 * ?MULTIPLIER;
coinbase_at_height(H) when H < 22 -> 10 * ?MULTIPLIER;
coinbase_at_height(H) when H < 24 -> 11 * ?MULTIPLIER;
coinbase_at_height(H) when H < 26 -> 12 * ?MULTIPLIER;
coinbase_at_height(H) when H < 28 -> 13 * ?MULTIPLIER;
coinbase_at_height(H) when H < 30 -> 14 * ?MULTIPLIER;
coinbase_at_height(H) when H < 32 -> 15 * ?MULTIPLIER;
coinbase_at_height(H) when H < 34 -> 16 * ?MULTIPLIER;
coinbase_at_height(H) when H < 36 -> 17 * ?MULTIPLIER;
coinbase_at_height(H) when H < 38 -> 18 * ?MULTIPLIER;
coinbase_at_height(H) when H < 40 -> 19 * ?MULTIPLIER;
coinbase_at_height(H) when H < 42 -> 20 * ?MULTIPLIER;
coinbase_at_height(H) when H < 44 -> 21 * ?MULTIPLIER;
coinbase_at_height(H) when H < 46 -> 22 * ?MULTIPLIER;
coinbase_at_height(H) when H < 48 -> 23 * ?MULTIPLIER;
coinbase_at_height(H) when H < 50 -> 24 * ?MULTIPLIER;
coinbase_at_height(H) when H < 52 -> 25 * ?MULTIPLIER;
coinbase_at_height(H) when H < 54 -> 26 * ?MULTIPLIER;
coinbase_at_height(H) when H < 56 -> 27 * ?MULTIPLIER;
coinbase_at_height(H) when H < 58 -> 28 * ?MULTIPLIER;
coinbase_at_height(H) when H < 60 -> 29 * ?MULTIPLIER;
coinbase_at_height(H) when H < 62 -> 30 * ?MULTIPLIER;
coinbase_at_height(H) when H < 64 -> 31 * ?MULTIPLIER;
coinbase_at_height(H) when H < 66 -> 32 * ?MULTIPLIER;
coinbase_at_height(H) when H < 68 -> 33 * ?MULTIPLIER;
coinbase_at_height(H) when H < 70 -> 34 * ?MULTIPLIER;
coinbase_at_height(H) when H < 72 -> 35 * ?MULTIPLIER;
coinbase_at_height(H) when H < 75 -> 36 * ?MULTIPLIER;
coinbase_at_height(H) when H < 77 -> 37 * ?MULTIPLIER;
coinbase_at_height(H) when H < 79 -> 38 * ?MULTIPLIER;
coinbase_at_height(H) when H < 81 -> 39 * ?MULTIPLIER;
coinbase_at_height(H) when H < 83 -> 40 * ?MULTIPLIER;
coinbase_at_height(H) when H < 85 -> 41 * ?MULTIPLIER;
coinbase_at_height(H) when H < 87 -> 42 * ?MULTIPLIER;
coinbase_at_height(H) when H < 89 -> 43 * ?MULTIPLIER;
coinbase_at_height(H) when H < 91 -> 44 * ?MULTIPLIER;
coinbase_at_height(H) when H < 93 -> 45 * ?MULTIPLIER;
coinbase_at_height(H) when H < 95 -> 46 * ?MULTIPLIER;
coinbase_at_height(H) when H < 97 -> 47 * ?MULTIPLIER;
coinbase_at_height(H) when H < 99 -> 48 * ?MULTIPLIER;
coinbase_at_height(H) when H < 101 -> 49 * ?MULTIPLIER;
coinbase_at_height(H) when H < 103 -> 50 * ?MULTIPLIER;
coinbase_at_height(H) when H < 105 -> 51 * ?MULTIPLIER;
coinbase_at_height(H) when H < 107 -> 52 * ?MULTIPLIER;
coinbase_at_height(H) when H < 109 -> 53 * ?MULTIPLIER;
coinbase_at_height(H) when H < 111 -> 54 * ?MULTIPLIER;
coinbase_at_height(H) when H < 113 -> 55 * ?MULTIPLIER;
coinbase_at_height(H) when H < 115 -> 56 * ?MULTIPLIER;
coinbase_at_height(H) when H < 117 -> 57 * ?MULTIPLIER;
coinbase_at_height(H) when H < 119 -> 58 * ?MULTIPLIER;
coinbase_at_height(H) when H < 121 -> 59 * ?MULTIPLIER;
coinbase_at_height(H) when H < 123 -> 60 * ?MULTIPLIER;
coinbase_at_height(H) when H < 125 -> 61 * ?MULTIPLIER;
coinbase_at_height(H) when H < 127 -> 62 * ?MULTIPLIER;
coinbase_at_height(H) when H < 129 -> 63 * ?MULTIPLIER;
coinbase_at_height(H) when H < 131 -> 64 * ?MULTIPLIER;
coinbase_at_height(H) when H < 133 -> 65 * ?MULTIPLIER;
coinbase_at_height(H) when H < 135 -> 66 * ?MULTIPLIER;
coinbase_at_height(H) when H < 137 -> 67 * ?MULTIPLIER;
coinbase_at_height(H) when H < 139 -> 68 * ?MULTIPLIER;
coinbase_at_height(H) when H < 141 -> 69 * ?MULTIPLIER;
coinbase_at_height(H) when H < 143 -> 70 * ?MULTIPLIER;
coinbase_at_height(H) when H < 145 -> 71 * ?MULTIPLIER;
coinbase_at_height(H) when H < 148 -> 72 * ?MULTIPLIER;
coinbase_at_height(H) when H < 150 -> 73 * ?MULTIPLIER;
coinbase_at_height(H) when H < 152 -> 74 * ?MULTIPLIER;
coinbase_at_height(H) when H < 154 -> 75 * ?MULTIPLIER;
coinbase_at_height(H) when H < 156 -> 76 * ?MULTIPLIER;
coinbase_at_height(H) when H < 158 -> 77 * ?MULTIPLIER;
coinbase_at_height(H) when H < 160 -> 78 * ?MULTIPLIER;
coinbase_at_height(H) when H < 162 -> 79 * ?MULTIPLIER;
coinbase_at_height(H) when H < 164 -> 80 * ?MULTIPLIER;
coinbase_at_height(H) when H < 166 -> 81 * ?MULTIPLIER;
coinbase_at_height(H) when H < 168 -> 82 * ?MULTIPLIER;
coinbase_at_height(H) when H < 170 -> 83 * ?MULTIPLIER;
coinbase_at_height(H) when H < 172 -> 84 * ?MULTIPLIER;
coinbase_at_height(H) when H < 174 -> 85 * ?MULTIPLIER;
coinbase_at_height(H) when H < 176 -> 86 * ?MULTIPLIER;
coinbase_at_height(H) when H < 178 -> 87 * ?MULTIPLIER;
coinbase_at_height(H) when H < 180 -> 88 * ?MULTIPLIER;
coinbase_at_height(H) when H < 182 -> 89 * ?MULTIPLIER;
coinbase_at_height(H) when H < 184 -> 90 * ?MULTIPLIER;
coinbase_at_height(H) when H < 186 -> 91 * ?MULTIPLIER;
coinbase_at_height(H) when H < 188 -> 92 * ?MULTIPLIER;
coinbase_at_height(H) when H < 190 -> 93 * ?MULTIPLIER;
coinbase_at_height(H) when H < 192 -> 94 * ?MULTIPLIER;
coinbase_at_height(H) when H < 194 -> 95 * ?MULTIPLIER;
coinbase_at_height(H) when H < 196 -> 96 * ?MULTIPLIER;
coinbase_at_height(H) when H < 198 -> 97 * ?MULTIPLIER;
coinbase_at_height(H) when H < 200 -> 98 * ?MULTIPLIER;
coinbase_at_height(H) when H < 202 -> 99 * ?MULTIPLIER;
coinbase_at_height(H) when H < 204 -> 100 * ?MULTIPLIER;
coinbase_at_height(H) when H < 206 -> 101 * ?MULTIPLIER;
coinbase_at_height(H) when H < 208 -> 102 * ?MULTIPLIER;
coinbase_at_height(H) when H < 210 -> 103 * ?MULTIPLIER;
coinbase_at_height(H) when H < 212 -> 104 * ?MULTIPLIER;
coinbase_at_height(H) when H < 214 -> 105 * ?MULTIPLIER;
coinbase_at_height(H) when H < 216 -> 106 * ?MULTIPLIER;
coinbase_at_height(H) when H < 219 -> 107 * ?MULTIPLIER;
coinbase_at_height(H) when H < 221 -> 108 * ?MULTIPLIER;
coinbase_at_height(H) when H < 223 -> 109 * ?MULTIPLIER;
coinbase_at_height(H) when H < 225 -> 110 * ?MULTIPLIER;
coinbase_at_height(H) when H < 227 -> 111 * ?MULTIPLIER;
coinbase_at_height(H) when H < 229 -> 112 * ?MULTIPLIER;
coinbase_at_height(H) when H < 231 -> 113 * ?MULTIPLIER;
coinbase_at_height(H) when H < 233 -> 114 * ?MULTIPLIER;
coinbase_at_height(H) when H < 235 -> 115 * ?MULTIPLIER;
coinbase_at_height(H) when H < 237 -> 116 * ?MULTIPLIER;
coinbase_at_height(H) when H < 239 -> 117 * ?MULTIPLIER;
coinbase_at_height(H) when H < 241 -> 118 * ?MULTIPLIER;
coinbase_at_height(H) when H < 243 -> 119 * ?MULTIPLIER;
coinbase_at_height(H) when H < 245 -> 120 * ?MULTIPLIER;
coinbase_at_height(H) when H < 247 -> 121 * ?MULTIPLIER;
coinbase_at_height(H) when H < 249 -> 122 * ?MULTIPLIER;
coinbase_at_height(H) when H < 251 -> 123 * ?MULTIPLIER;
coinbase_at_height(H) when H < 253 -> 124 * ?MULTIPLIER;
coinbase_at_height(H) when H < 255 -> 125 * ?MULTIPLIER;
coinbase_at_height(H) when H < 257 -> 126 * ?MULTIPLIER;
coinbase_at_height(H) when H < 259 -> 127 * ?MULTIPLIER;
coinbase_at_height(H) when H < 261 -> 128 * ?MULTIPLIER;
coinbase_at_height(H) when H < 263 -> 129 * ?MULTIPLIER;
coinbase_at_height(H) when H < 265 -> 130 * ?MULTIPLIER;
coinbase_at_height(H) when H < 267 -> 131 * ?MULTIPLIER;
coinbase_at_height(H) when H < 269 -> 132 * ?MULTIPLIER;
coinbase_at_height(H) when H < 271 -> 133 * ?MULTIPLIER;
coinbase_at_height(H) when H < 273 -> 134 * ?MULTIPLIER;
coinbase_at_height(H) when H < 275 -> 135 * ?MULTIPLIER;
coinbase_at_height(H) when H < 277 -> 136 * ?MULTIPLIER;
coinbase_at_height(H) when H < 279 -> 137 * ?MULTIPLIER;
coinbase_at_height(H) when H < 281 -> 138 * ?MULTIPLIER;
coinbase_at_height(H) when H < 283 -> 139 * ?MULTIPLIER;
coinbase_at_height(H) when H < 285 -> 140 * ?MULTIPLIER;
coinbase_at_height(H) when H < 287 -> 141 * ?MULTIPLIER;
coinbase_at_height(H) when H < 289 -> 142 * ?MULTIPLIER;
coinbase_at_height(H) when H < 291 -> 143 * ?MULTIPLIER;
coinbase_at_height(H) when H < 294 -> 144 * ?MULTIPLIER;
coinbase_at_height(H) when H < 296 -> 145 * ?MULTIPLIER;
coinbase_at_height(H) when H < 298 -> 146 * ?MULTIPLIER;
coinbase_at_height(H) when H < 300 -> 147 * ?MULTIPLIER;
coinbase_at_height(H) when H < 302 -> 148 * ?MULTIPLIER;
coinbase_at_height(H) when H < 304 -> 149 * ?MULTIPLIER;
coinbase_at_height(H) when H < 306 -> 150 * ?MULTIPLIER;
coinbase_at_height(H) when H < 308 -> 151 * ?MULTIPLIER;
coinbase_at_height(H) when H < 310 -> 152 * ?MULTIPLIER;
coinbase_at_height(H) when H < 312 -> 153 * ?MULTIPLIER;
coinbase_at_height(H) when H < 314 -> 154 * ?MULTIPLIER;
coinbase_at_height(H) when H < 316 -> 155 * ?MULTIPLIER;
coinbase_at_height(H) when H < 318 -> 156 * ?MULTIPLIER;
coinbase_at_height(H) when H < 320 -> 157 * ?MULTIPLIER;
coinbase_at_height(H) when H < 322 -> 158 * ?MULTIPLIER;
coinbase_at_height(H) when H < 324 -> 159 * ?MULTIPLIER;
coinbase_at_height(H) when H < 326 -> 160 * ?MULTIPLIER;
coinbase_at_height(H) when H < 328 -> 161 * ?MULTIPLIER;
coinbase_at_height(H) when H < 330 -> 162 * ?MULTIPLIER;
coinbase_at_height(H) when H < 332 -> 163 * ?MULTIPLIER;
coinbase_at_height(H) when H < 334 -> 164 * ?MULTIPLIER;
coinbase_at_height(H) when H < 336 -> 165 * ?MULTIPLIER;
coinbase_at_height(H) when H < 338 -> 166 * ?MULTIPLIER;
coinbase_at_height(H) when H < 340 -> 167 * ?MULTIPLIER;
coinbase_at_height(H) when H < 342 -> 168 * ?MULTIPLIER;
coinbase_at_height(H) when H < 344 -> 169 * ?MULTIPLIER;
coinbase_at_height(H) when H < 346 -> 170 * ?MULTIPLIER;
coinbase_at_height(H) when H < 348 -> 171 * ?MULTIPLIER;
coinbase_at_height(H) when H < 350 -> 172 * ?MULTIPLIER;
coinbase_at_height(H) when H < 352 -> 173 * ?MULTIPLIER;
coinbase_at_height(H) when H < 354 -> 174 * ?MULTIPLIER;
coinbase_at_height(H) when H < 356 -> 175 * ?MULTIPLIER;
coinbase_at_height(H) when H < 358 -> 176 * ?MULTIPLIER;
coinbase_at_height(H) when H < 360 -> 177 * ?MULTIPLIER;
coinbase_at_height(H) when H < 362 -> 178 * ?MULTIPLIER;
coinbase_at_height(H) when H < 364 -> 179 * ?MULTIPLIER;
coinbase_at_height(H) when H < 367 -> 180 * ?MULTIPLIER;
coinbase_at_height(H) when H < 369 -> 181 * ?MULTIPLIER;
coinbase_at_height(H) when H < 371 -> 182 * ?MULTIPLIER;
coinbase_at_height(H) when H < 373 -> 183 * ?MULTIPLIER;
coinbase_at_height(H) when H < 375 -> 184 * ?MULTIPLIER;
coinbase_at_height(H) when H < 377 -> 185 * ?MULTIPLIER;
coinbase_at_height(H) when H < 379 -> 186 * ?MULTIPLIER;
coinbase_at_height(H) when H < 381 -> 187 * ?MULTIPLIER;
coinbase_at_height(H) when H < 383 -> 188 * ?MULTIPLIER;
coinbase_at_height(H) when H < 385 -> 189 * ?MULTIPLIER;
coinbase_at_height(H) when H < 387 -> 190 * ?MULTIPLIER;
coinbase_at_height(H) when H < 389 -> 191 * ?MULTIPLIER;
coinbase_at_height(H) when H < 391 -> 192 * ?MULTIPLIER;
coinbase_at_height(H) when H < 393 -> 193 * ?MULTIPLIER;
coinbase_at_height(H) when H < 395 -> 194 * ?MULTIPLIER;
coinbase_at_height(H) when H < 397 -> 195 * ?MULTIPLIER;
coinbase_at_height(H) when H < 399 -> 196 * ?MULTIPLIER;
coinbase_at_height(H) when H < 401 -> 197 * ?MULTIPLIER;
coinbase_at_height(H) when H < 403 -> 198 * ?MULTIPLIER;
coinbase_at_height(H) when H < 405 -> 199 * ?MULTIPLIER;
coinbase_at_height(H) when H < 407 -> 200 * ?MULTIPLIER;
coinbase_at_height(H) when H < 409 -> 201 * ?MULTIPLIER;
coinbase_at_height(H) when H < 411 -> 202 * ?MULTIPLIER;
coinbase_at_height(H) when H < 413 -> 203 * ?MULTIPLIER;
coinbase_at_height(H) when H < 415 -> 204 * ?MULTIPLIER;
coinbase_at_height(H) when H < 417 -> 205 * ?MULTIPLIER;
coinbase_at_height(H) when H < 419 -> 206 * ?MULTIPLIER;
coinbase_at_height(H) when H < 421 -> 207 * ?MULTIPLIER;
coinbase_at_height(H) when H < 423 -> 208 * ?MULTIPLIER;
coinbase_at_height(H) when H < 425 -> 209 * ?MULTIPLIER;
coinbase_at_height(H) when H < 427 -> 210 * ?MULTIPLIER;
coinbase_at_height(H) when H < 429 -> 211 * ?MULTIPLIER;
coinbase_at_height(H) when H < 431 -> 212 * ?MULTIPLIER;
coinbase_at_height(H) when H < 433 -> 213 * ?MULTIPLIER;
coinbase_at_height(H) when H < 435 -> 214 * ?MULTIPLIER;
coinbase_at_height(H) when H < 437 -> 215 * ?MULTIPLIER;
coinbase_at_height(H) when H < 439 -> 216 * ?MULTIPLIER;
coinbase_at_height(H) when H < 442 -> 217 * ?MULTIPLIER;
coinbase_at_height(H) when H < 444 -> 218 * ?MULTIPLIER;
coinbase_at_height(H) when H < 446 -> 219 * ?MULTIPLIER;
coinbase_at_height(H) when H < 448 -> 220 * ?MULTIPLIER;
coinbase_at_height(H) when H < 450 -> 221 * ?MULTIPLIER;
coinbase_at_height(H) when H < 452 -> 222 * ?MULTIPLIER;
coinbase_at_height(H) when H < 454 -> 223 * ?MULTIPLIER;
coinbase_at_height(H) when H < 456 -> 224 * ?MULTIPLIER;
coinbase_at_height(H) when H < 458 -> 225 * ?MULTIPLIER;
coinbase_at_height(H) when H < 460 -> 226 * ?MULTIPLIER;
coinbase_at_height(H) when H < 462 -> 227 * ?MULTIPLIER;
coinbase_at_height(H) when H < 464 -> 228 * ?MULTIPLIER;
coinbase_at_height(H) when H < 466 -> 229 * ?MULTIPLIER;
coinbase_at_height(H) when H < 468 -> 230 * ?MULTIPLIER;
coinbase_at_height(H) when H < 470 -> 231 * ?MULTIPLIER;
coinbase_at_height(H) when H < 472 -> 232 * ?MULTIPLIER;
coinbase_at_height(H) when H < 474 -> 233 * ?MULTIPLIER;
coinbase_at_height(H) when H < 476 -> 234 * ?MULTIPLIER;
coinbase_at_height(H) when H < 478 -> 235 * ?MULTIPLIER;
coinbase_at_height(H) when H < 480 -> 236 * ?MULTIPLIER;
coinbase_at_height(H) when H < 482 -> 237 * ?MULTIPLIER;
coinbase_at_height(H) when H < 484 -> 238 * ?MULTIPLIER;
coinbase_at_height(H) when H < 486 -> 239 * ?MULTIPLIER;
coinbase_at_height(H) when H < 488 -> 240 * ?MULTIPLIER;
coinbase_at_height(H) when H < 490 -> 241 * ?MULTIPLIER;
coinbase_at_height(H) when H < 492 -> 242 * ?MULTIPLIER;
coinbase_at_height(H) when H < 494 -> 243 * ?MULTIPLIER;
coinbase_at_height(H) when H < 496 -> 244 * ?MULTIPLIER;
coinbase_at_height(H) when H < 498 -> 245 * ?MULTIPLIER;
coinbase_at_height(H) when H < 500 -> 246 * ?MULTIPLIER;
coinbase_at_height(H) when H < 502 -> 247 * ?MULTIPLIER;
coinbase_at_height(H) when H < 504 -> 248 * ?MULTIPLIER;
coinbase_at_height(H) when H < 506 -> 249 * ?MULTIPLIER;
coinbase_at_height(H) when H < 508 -> 250 * ?MULTIPLIER;
coinbase_at_height(H) when H < 510 -> 251 * ?MULTIPLIER;
coinbase_at_height(H) when H < 512 -> 252 * ?MULTIPLIER;
coinbase_at_height(H) when H < 514 -> 253 * ?MULTIPLIER;
coinbase_at_height(H) when H < 517 -> 254 * ?MULTIPLIER;
coinbase_at_height(H) when H < 519 -> 255 * ?MULTIPLIER;
coinbase_at_height(H) when H < 521 -> 256 * ?MULTIPLIER;
coinbase_at_height(H) when H < 523 -> 257 * ?MULTIPLIER;
coinbase_at_height(H) when H < 525 -> 258 * ?MULTIPLIER;
coinbase_at_height(H) when H < 527 -> 259 * ?MULTIPLIER;
coinbase_at_height(H) when H < 529 -> 260 * ?MULTIPLIER;
coinbase_at_height(H) when H < 531 -> 261 * ?MULTIPLIER;
coinbase_at_height(H) when H < 533 -> 262 * ?MULTIPLIER;
coinbase_at_height(H) when H < 535 -> 263 * ?MULTIPLIER;
coinbase_at_height(H) when H < 537 -> 264 * ?MULTIPLIER;
coinbase_at_height(H) when H < 539 -> 265 * ?MULTIPLIER;
coinbase_at_height(H) when H < 541 -> 266 * ?MULTIPLIER;
coinbase_at_height(H) when H < 543 -> 267 * ?MULTIPLIER;
coinbase_at_height(H) when H < 545 -> 268 * ?MULTIPLIER;
coinbase_at_height(H) when H < 547 -> 269 * ?MULTIPLIER;
coinbase_at_height(H) when H < 549 -> 270 * ?MULTIPLIER;
coinbase_at_height(H) when H < 551 -> 271 * ?MULTIPLIER;
coinbase_at_height(H) when H < 553 -> 272 * ?MULTIPLIER;
coinbase_at_height(H) when H < 555 -> 273 * ?MULTIPLIER;
coinbase_at_height(H) when H < 557 -> 274 * ?MULTIPLIER;
coinbase_at_height(H) when H < 559 -> 275 * ?MULTIPLIER;
coinbase_at_height(H) when H < 561 -> 276 * ?MULTIPLIER;
coinbase_at_height(H) when H < 563 -> 277 * ?MULTIPLIER;
coinbase_at_height(H) when H < 565 -> 278 * ?MULTIPLIER;
coinbase_at_height(H) when H < 567 -> 279 * ?MULTIPLIER;
coinbase_at_height(H) when H < 569 -> 280 * ?MULTIPLIER;
coinbase_at_height(H) when H < 571 -> 281 * ?MULTIPLIER;
coinbase_at_height(H) when H < 573 -> 282 * ?MULTIPLIER;
coinbase_at_height(H) when H < 575 -> 283 * ?MULTIPLIER;
coinbase_at_height(H) when H < 577 -> 284 * ?MULTIPLIER;
coinbase_at_height(H) when H < 579 -> 285 * ?MULTIPLIER;
coinbase_at_height(H) when H < 581 -> 286 * ?MULTIPLIER;
coinbase_at_height(H) when H < 583 -> 287 * ?MULTIPLIER;
coinbase_at_height(H) when H < 585 -> 288 * ?MULTIPLIER;
coinbase_at_height(H) when H < 587 -> 289 * ?MULTIPLIER;
coinbase_at_height(H) when H < 589 -> 290 * ?MULTIPLIER;
coinbase_at_height(H) when H < 591 -> 291 * ?MULTIPLIER;
coinbase_at_height(H) when H < 594 -> 292 * ?MULTIPLIER;
coinbase_at_height(H) when H < 596 -> 293 * ?MULTIPLIER;
coinbase_at_height(H) when H < 598 -> 294 * ?MULTIPLIER;
coinbase_at_height(H) when H < 600 -> 295 * ?MULTIPLIER;
coinbase_at_height(H) when H < 602 -> 296 * ?MULTIPLIER;
coinbase_at_height(H) when H < 604 -> 297 * ?MULTIPLIER;
coinbase_at_height(H) when H < 606 -> 298 * ?MULTIPLIER;
coinbase_at_height(H) when H < 608 -> 299 * ?MULTIPLIER;
coinbase_at_height(H) when H < 610 -> 300 * ?MULTIPLIER;
coinbase_at_height(H) when H < 612 -> 301 * ?MULTIPLIER;
coinbase_at_height(H) when H < 614 -> 302 * ?MULTIPLIER;
coinbase_at_height(H) when H < 616 -> 303 * ?MULTIPLIER;
coinbase_at_height(H) when H < 618 -> 304 * ?MULTIPLIER;
coinbase_at_height(H) when H < 620 -> 305 * ?MULTIPLIER;
coinbase_at_height(H) when H < 622 -> 306 * ?MULTIPLIER;
coinbase_at_height(H) when H < 624 -> 307 * ?MULTIPLIER;
coinbase_at_height(H) when H < 626 -> 308 * ?MULTIPLIER;
coinbase_at_height(H) when H < 628 -> 309 * ?MULTIPLIER;
coinbase_at_height(H) when H < 630 -> 310 * ?MULTIPLIER;
coinbase_at_height(H) when H < 632 -> 311 * ?MULTIPLIER;
coinbase_at_height(H) when H < 634 -> 312 * ?MULTIPLIER;
coinbase_at_height(H) when H < 636 -> 313 * ?MULTIPLIER;
coinbase_at_height(H) when H < 638 -> 314 * ?MULTIPLIER;
coinbase_at_height(H) when H < 640 -> 315 * ?MULTIPLIER;
coinbase_at_height(H) when H < 642 -> 316 * ?MULTIPLIER;
coinbase_at_height(H) when H < 644 -> 317 * ?MULTIPLIER;
coinbase_at_height(H) when H < 646 -> 318 * ?MULTIPLIER;
coinbase_at_height(H) when H < 648 -> 319 * ?MULTIPLIER;
coinbase_at_height(H) when H < 650 -> 320 * ?MULTIPLIER;
coinbase_at_height(H) when H < 652 -> 321 * ?MULTIPLIER;
coinbase_at_height(H) when H < 654 -> 322 * ?MULTIPLIER;
coinbase_at_height(H) when H < 656 -> 323 * ?MULTIPLIER;
coinbase_at_height(H) when H < 658 -> 324 * ?MULTIPLIER;
coinbase_at_height(H) when H < 660 -> 325 * ?MULTIPLIER;
coinbase_at_height(H) when H < 662 -> 326 * ?MULTIPLIER;
coinbase_at_height(H) when H < 664 -> 327 * ?MULTIPLIER;
coinbase_at_height(H) when H < 666 -> 328 * ?MULTIPLIER;
coinbase_at_height(H) when H < 668 -> 329 * ?MULTIPLIER;
coinbase_at_height(H) when H < 670 -> 330 * ?MULTIPLIER;
coinbase_at_height(H) when H < 673 -> 331 * ?MULTIPLIER;
coinbase_at_height(H) when H < 675 -> 332 * ?MULTIPLIER;
coinbase_at_height(H) when H < 677 -> 333 * ?MULTIPLIER;
coinbase_at_height(H) when H < 679 -> 334 * ?MULTIPLIER;
coinbase_at_height(H) when H < 681 -> 335 * ?MULTIPLIER;
coinbase_at_height(H) when H < 683 -> 336 * ?MULTIPLIER;
coinbase_at_height(H) when H < 685 -> 337 * ?MULTIPLIER;
coinbase_at_height(H) when H < 687 -> 338 * ?MULTIPLIER;
coinbase_at_height(H) when H < 689 -> 339 * ?MULTIPLIER;
coinbase_at_height(H) when H < 691 -> 340 * ?MULTIPLIER;
coinbase_at_height(H) when H < 693 -> 341 * ?MULTIPLIER;
coinbase_at_height(H) when H < 695 -> 342 * ?MULTIPLIER;
coinbase_at_height(H) when H < 697 -> 343 * ?MULTIPLIER;
coinbase_at_height(H) when H < 699 -> 344 * ?MULTIPLIER;
coinbase_at_height(H) when H < 701 -> 345 * ?MULTIPLIER;
coinbase_at_height(H) when H < 703 -> 346 * ?MULTIPLIER;
coinbase_at_height(H) when H < 705 -> 347 * ?MULTIPLIER;
coinbase_at_height(H) when H < 707 -> 348 * ?MULTIPLIER;
coinbase_at_height(H) when H < 709 -> 349 * ?MULTIPLIER;
coinbase_at_height(H) when H < 711 -> 350 * ?MULTIPLIER;
coinbase_at_height(H) when H < 713 -> 351 * ?MULTIPLIER;
coinbase_at_height(H) when H < 715 -> 352 * ?MULTIPLIER;
coinbase_at_height(H) when H < 717 -> 353 * ?MULTIPLIER;
coinbase_at_height(H) when H < 719 -> 354 * ?MULTIPLIER;
coinbase_at_height(H) when H < 721 -> 355 * ?MULTIPLIER;
coinbase_at_height(H) when H < 723 -> 356 * ?MULTIPLIER;
coinbase_at_height(H) when H < 725 -> 357 * ?MULTIPLIER;
coinbase_at_height(H) when H < 727 -> 358 * ?MULTIPLIER;
coinbase_at_height(H) when H < 729 -> 359 * ?MULTIPLIER;
coinbase_at_height(H) when H < 731 -> 360 * ?MULTIPLIER;
coinbase_at_height(H) when H < 733 -> 361 * ?MULTIPLIER;
coinbase_at_height(H) when H < 735 -> 362 * ?MULTIPLIER;
coinbase_at_height(H) when H < 737 -> 363 * ?MULTIPLIER;
coinbase_at_height(H) when H < 739 -> 364 * ?MULTIPLIER;
coinbase_at_height(H) when H < 741 -> 365 * ?MULTIPLIER;
coinbase_at_height(H) when H < 743 -> 366 * ?MULTIPLIER;
coinbase_at_height(H) when H < 745 -> 367 * ?MULTIPLIER;
coinbase_at_height(H) when H < 747 -> 368 * ?MULTIPLIER;
coinbase_at_height(H) when H < 749 -> 369 * ?MULTIPLIER;
coinbase_at_height(H) when H < 751 -> 370 * ?MULTIPLIER;
coinbase_at_height(H) when H < 754 -> 371 * ?MULTIPLIER;
coinbase_at_height(H) when H < 756 -> 372 * ?MULTIPLIER;
coinbase_at_height(H) when H < 758 -> 373 * ?MULTIPLIER;
coinbase_at_height(H) when H < 760 -> 374 * ?MULTIPLIER;
coinbase_at_height(H) when H < 762 -> 375 * ?MULTIPLIER;
coinbase_at_height(H) when H < 764 -> 376 * ?MULTIPLIER;
coinbase_at_height(H) when H < 766 -> 377 * ?MULTIPLIER;
coinbase_at_height(H) when H < 768 -> 378 * ?MULTIPLIER;
coinbase_at_height(H) when H < 770 -> 379 * ?MULTIPLIER;
coinbase_at_height(H) when H < 772 -> 380 * ?MULTIPLIER;
coinbase_at_height(H) when H < 774 -> 381 * ?MULTIPLIER;
coinbase_at_height(H) when H < 776 -> 382 * ?MULTIPLIER;
coinbase_at_height(H) when H < 778 -> 383 * ?MULTIPLIER;
coinbase_at_height(H) when H < 780 -> 384 * ?MULTIPLIER;
coinbase_at_height(H) when H < 782 -> 385 * ?MULTIPLIER;
coinbase_at_height(H) when H < 784 -> 386 * ?MULTIPLIER;
coinbase_at_height(H) when H < 786 -> 387 * ?MULTIPLIER;
coinbase_at_height(H) when H < 788 -> 388 * ?MULTIPLIER;
coinbase_at_height(H) when H < 790 -> 389 * ?MULTIPLIER;
coinbase_at_height(H) when H < 792 -> 390 * ?MULTIPLIER;
coinbase_at_height(H) when H < 794 -> 391 * ?MULTIPLIER;
coinbase_at_height(H) when H < 796 -> 392 * ?MULTIPLIER;
coinbase_at_height(H) when H < 798 -> 393 * ?MULTIPLIER;
coinbase_at_height(H) when H < 800 -> 394 * ?MULTIPLIER;
coinbase_at_height(H) when H < 802 -> 395 * ?MULTIPLIER;
coinbase_at_height(H) when H < 804 -> 396 * ?MULTIPLIER;
coinbase_at_height(H) when H < 806 -> 397 * ?MULTIPLIER;
coinbase_at_height(H) when H < 808 -> 398 * ?MULTIPLIER;
coinbase_at_height(H) when H < 810 -> 399 * ?MULTIPLIER;
coinbase_at_height(H) when H < 812 -> 400 * ?MULTIPLIER;
coinbase_at_height(H) when H < 814 -> 401 * ?MULTIPLIER;
coinbase_at_height(H) when H < 816 -> 402 * ?MULTIPLIER;
coinbase_at_height(H) when H < 818 -> 403 * ?MULTIPLIER;
coinbase_at_height(H) when H < 820 -> 404 * ?MULTIPLIER;
coinbase_at_height(H) when H < 822 -> 405 * ?MULTIPLIER;
coinbase_at_height(H) when H < 824 -> 406 * ?MULTIPLIER;
coinbase_at_height(H) when H < 826 -> 407 * ?MULTIPLIER;
coinbase_at_height(H) when H < 828 -> 408 * ?MULTIPLIER;
coinbase_at_height(H) when H < 830 -> 409 * ?MULTIPLIER;
coinbase_at_height(H) when H < 832 -> 410 * ?MULTIPLIER;
coinbase_at_height(H) when H < 835 -> 411 * ?MULTIPLIER;
coinbase_at_height(H) when H < 837 -> 412 * ?MULTIPLIER;
coinbase_at_height(H) when H < 839 -> 413 * ?MULTIPLIER;
coinbase_at_height(H) when H < 841 -> 414 * ?MULTIPLIER;
coinbase_at_height(H) when H < 843 -> 415 * ?MULTIPLIER;
coinbase_at_height(H) when H < 845 -> 416 * ?MULTIPLIER;
coinbase_at_height(H) when H < 847 -> 417 * ?MULTIPLIER;
coinbase_at_height(H) when H < 849 -> 418 * ?MULTIPLIER;
coinbase_at_height(H) when H < 851 -> 419 * ?MULTIPLIER;
coinbase_at_height(H) when H < 853 -> 420 * ?MULTIPLIER;
coinbase_at_height(H) when H < 855 -> 421 * ?MULTIPLIER;
coinbase_at_height(H) when H < 857 -> 422 * ?MULTIPLIER;
coinbase_at_height(H) when H < 859 -> 423 * ?MULTIPLIER;
coinbase_at_height(H) when H < 861 -> 424 * ?MULTIPLIER;
coinbase_at_height(H) when H < 863 -> 425 * ?MULTIPLIER;
coinbase_at_height(H) when H < 865 -> 426 * ?MULTIPLIER;
coinbase_at_height(H) when H < 867 -> 427 * ?MULTIPLIER;
coinbase_at_height(H) when H < 869 -> 428 * ?MULTIPLIER;
coinbase_at_height(H) when H < 871 -> 429 * ?MULTIPLIER;
coinbase_at_height(H) when H < 873 -> 430 * ?MULTIPLIER;
coinbase_at_height(H) when H < 875 -> 431 * ?MULTIPLIER;
coinbase_at_height(H) when H < 877 -> 432 * ?MULTIPLIER;
coinbase_at_height(H) when H < 879 -> 433 * ?MULTIPLIER;
coinbase_at_height(H) when H < 881 -> 434 * ?MULTIPLIER;
coinbase_at_height(H) when H < 883 -> 435 * ?MULTIPLIER;
coinbase_at_height(H) when H < 885 -> 436 * ?MULTIPLIER;
coinbase_at_height(H) when H < 887 -> 437 * ?MULTIPLIER;
coinbase_at_height(H) when H < 889 -> 438 * ?MULTIPLIER;
coinbase_at_height(H) when H < 891 -> 439 * ?MULTIPLIER;
coinbase_at_height(H) when H < 893 -> 440 * ?MULTIPLIER;
coinbase_at_height(H) when H < 895 -> 441 * ?MULTIPLIER;
coinbase_at_height(H) when H < 897 -> 442 * ?MULTIPLIER;
coinbase_at_height(H) when H < 899 -> 443 * ?MULTIPLIER;
coinbase_at_height(H) when H < 901 -> 444 * ?MULTIPLIER;
coinbase_at_height(H) when H < 903 -> 445 * ?MULTIPLIER;
coinbase_at_height(H) when H < 905 -> 446 * ?MULTIPLIER;
coinbase_at_height(H) when H < 907 -> 447 * ?MULTIPLIER;
coinbase_at_height(H) when H < 909 -> 448 * ?MULTIPLIER;
coinbase_at_height(H) when H < 911 -> 449 * ?MULTIPLIER;
coinbase_at_height(H) when H < 913 -> 450 * ?MULTIPLIER;
coinbase_at_height(H) when H < 915 -> 451 * ?MULTIPLIER;
coinbase_at_height(H) when H < 917 -> 452 * ?MULTIPLIER;
coinbase_at_height(H) when H < 920 -> 453 * ?MULTIPLIER;
coinbase_at_height(H) when H < 922 -> 454 * ?MULTIPLIER;
coinbase_at_height(H) when H < 924 -> 455 * ?MULTIPLIER;
coinbase_at_height(H) when H < 926 -> 456 * ?MULTIPLIER;
coinbase_at_height(H) when H < 928 -> 457 * ?MULTIPLIER;
coinbase_at_height(H) when H < 930 -> 458 * ?MULTIPLIER;
coinbase_at_height(H) when H < 932 -> 459 * ?MULTIPLIER;
coinbase_at_height(H) when H < 934 -> 460 * ?MULTIPLIER;
coinbase_at_height(H) when H < 936 -> 461 * ?MULTIPLIER;
coinbase_at_height(H) when H < 938 -> 462 * ?MULTIPLIER;
coinbase_at_height(H) when H < 940 -> 463 * ?MULTIPLIER;
coinbase_at_height(H) when H < 942 -> 464 * ?MULTIPLIER;
coinbase_at_height(H) when H < 944 -> 465 * ?MULTIPLIER;
coinbase_at_height(H) when H < 946 -> 466 * ?MULTIPLIER;
coinbase_at_height(H) when H < 948 -> 467 * ?MULTIPLIER;
coinbase_at_height(H) when H < 950 -> 468 * ?MULTIPLIER;
coinbase_at_height(H) when H < 952 -> 469 * ?MULTIPLIER;
coinbase_at_height(H) when H < 954 -> 470 * ?MULTIPLIER;
coinbase_at_height(H) when H < 956 -> 471 * ?MULTIPLIER;
coinbase_at_height(H) when H < 958 -> 472 * ?MULTIPLIER;
coinbase_at_height(H) when H < 3997 -> 473 * ?MULTIPLIER;
coinbase_at_height(H) when H < 5495 -> 472 * ?MULTIPLIER;
coinbase_at_height(H) when H < 6736 -> 471 * ?MULTIPLIER;
coinbase_at_height(H) when H < 7844 -> 470 * ?MULTIPLIER;
coinbase_at_height(H) when H < 8867 -> 469 * ?MULTIPLIER;
coinbase_at_height(H) when H < 9829 -> 468 * ?MULTIPLIER;
coinbase_at_height(H) when H < 10745 -> 467 * ?MULTIPLIER;
coinbase_at_height(H) when H < 11624 -> 466 * ?MULTIPLIER;
coinbase_at_height(H) when H < 12473 -> 465 * ?MULTIPLIER;
coinbase_at_height(H) when H < 13297 -> 464 * ?MULTIPLIER;
coinbase_at_height(H) when H < 14099 -> 463 * ?MULTIPLIER;
coinbase_at_height(H) when H < 14883 -> 462 * ?MULTIPLIER;
coinbase_at_height(H) when H < 15651 -> 461 * ?MULTIPLIER;
coinbase_at_height(H) when H < 16404 -> 460 * ?MULTIPLIER;
coinbase_at_height(H) when H < 17144 -> 459 * ?MULTIPLIER;
coinbase_at_height(H) when H < 17874 -> 458 * ?MULTIPLIER;
coinbase_at_height(H) when H < 18592 -> 457 * ?MULTIPLIER;
coinbase_at_height(H) when H < 19302 -> 456 * ?MULTIPLIER;
coinbase_at_height(H) when H < 20003 -> 455 * ?MULTIPLIER;
coinbase_at_height(H) when H < 20697 -> 454 * ?MULTIPLIER;
coinbase_at_height(H) when H < 21383 -> 453 * ?MULTIPLIER;
coinbase_at_height(H) when H < 22063 -> 452 * ?MULTIPLIER;
coinbase_at_height(H) when H < 22737 -> 451 * ?MULTIPLIER;
coinbase_at_height(H) when H < 23406 -> 450 * ?MULTIPLIER;
coinbase_at_height(H) when H < 24069 -> 449 * ?MULTIPLIER;
coinbase_at_height(H) when H < 24728 -> 448 * ?MULTIPLIER;
coinbase_at_height(H) when H < 25383 -> 447 * ?MULTIPLIER;
coinbase_at_height(H) when H < 26033 -> 446 * ?MULTIPLIER;
coinbase_at_height(H) when H < 26680 -> 445 * ?MULTIPLIER;
coinbase_at_height(H) when H < 27323 -> 444 * ?MULTIPLIER;
coinbase_at_height(H) when H < 27963 -> 443 * ?MULTIPLIER;
coinbase_at_height(H) when H < 28600 -> 442 * ?MULTIPLIER;
coinbase_at_height(H) when H < 29235 -> 441 * ?MULTIPLIER;
coinbase_at_height(H) when H < 29866 -> 440 * ?MULTIPLIER;
coinbase_at_height(H) when H < 30496 -> 439 * ?MULTIPLIER;
coinbase_at_height(H) when H < 31123 -> 438 * ?MULTIPLIER;
coinbase_at_height(H) when H < 31748 -> 437 * ?MULTIPLIER;
coinbase_at_height(H) when H < 32371 -> 436 * ?MULTIPLIER;
coinbase_at_height(H) when H < 32993 -> 435 * ?MULTIPLIER;
coinbase_at_height(H) when H < 33612 -> 434 * ?MULTIPLIER;
coinbase_at_height(H) when H < 34231 -> 433 * ?MULTIPLIER;
coinbase_at_height(H) when H < 34847 -> 432 * ?MULTIPLIER;
coinbase_at_height(H) when H < 35463 -> 431 * ?MULTIPLIER;
coinbase_at_height(H) when H < 36077 -> 430 * ?MULTIPLIER;
coinbase_at_height(H) when H < 36691 -> 429 * ?MULTIPLIER;
coinbase_at_height(H) when H < 37303 -> 428 * ?MULTIPLIER;
coinbase_at_height(H) when H < 37915 -> 427 * ?MULTIPLIER;
coinbase_at_height(H) when H < 38525 -> 426 * ?MULTIPLIER;
coinbase_at_height(H) when H < 39135 -> 425 * ?MULTIPLIER;
coinbase_at_height(H) when H < 39745 -> 424 * ?MULTIPLIER;
coinbase_at_height(H) when H < 40354 -> 423 * ?MULTIPLIER;
coinbase_at_height(H) when H < 40962 -> 422 * ?MULTIPLIER;
coinbase_at_height(H) when H < 41570 -> 421 * ?MULTIPLIER;
coinbase_at_height(H) when H < 42178 -> 420 * ?MULTIPLIER;
coinbase_at_height(H) when H < 42786 -> 419 * ?MULTIPLIER;
coinbase_at_height(H) when H < 43393 -> 418 * ?MULTIPLIER;
coinbase_at_height(H) when H < 44000 -> 417 * ?MULTIPLIER;
coinbase_at_height(H) when H < 44607 -> 416 * ?MULTIPLIER;
coinbase_at_height(H) when H < 45215 -> 415 * ?MULTIPLIER;
coinbase_at_height(H) when H < 45822 -> 414 * ?MULTIPLIER;
coinbase_at_height(H) when H < 46429 -> 413 * ?MULTIPLIER;
coinbase_at_height(H) when H < 47037 -> 412 * ?MULTIPLIER;
coinbase_at_height(H) when H < 47644 -> 411 * ?MULTIPLIER;
coinbase_at_height(H) when H < 48252 -> 410 * ?MULTIPLIER;
coinbase_at_height(H) when H < 48861 -> 409 * ?MULTIPLIER;
coinbase_at_height(H) when H < 49469 -> 408 * ?MULTIPLIER;
coinbase_at_height(H) when H < 50079 -> 407 * ?MULTIPLIER;
coinbase_at_height(H) when H < 50688 -> 406 * ?MULTIPLIER;
coinbase_at_height(H) when H < 51298 -> 405 * ?MULTIPLIER;
coinbase_at_height(H) when H < 51909 -> 404 * ?MULTIPLIER;
coinbase_at_height(H) when H < 52520 -> 403 * ?MULTIPLIER;
coinbase_at_height(H) when H < 53132 -> 402 * ?MULTIPLIER;
coinbase_at_height(H) when H < 53745 -> 401 * ?MULTIPLIER;
coinbase_at_height(H) when H < 54358 -> 400 * ?MULTIPLIER;
coinbase_at_height(H) when H < 54972 -> 399 * ?MULTIPLIER;
coinbase_at_height(H) when H < 55587 -> 398 * ?MULTIPLIER;
coinbase_at_height(H) when H < 56202 -> 397 * ?MULTIPLIER;
coinbase_at_height(H) when H < 56819 -> 396 * ?MULTIPLIER;
coinbase_at_height(H) when H < 57436 -> 395 * ?MULTIPLIER;
coinbase_at_height(H) when H < 58055 -> 394 * ?MULTIPLIER;
coinbase_at_height(H) when H < 58674 -> 393 * ?MULTIPLIER;
coinbase_at_height(H) when H < 59295 -> 392 * ?MULTIPLIER;
coinbase_at_height(H) when H < 59916 -> 391 * ?MULTIPLIER;
coinbase_at_height(H) when H < 60539 -> 390 * ?MULTIPLIER;
coinbase_at_height(H) when H < 61162 -> 389 * ?MULTIPLIER;
coinbase_at_height(H) when H < 61787 -> 388 * ?MULTIPLIER;
coinbase_at_height(H) when H < 62413 -> 387 * ?MULTIPLIER;
coinbase_at_height(H) when H < 63041 -> 386 * ?MULTIPLIER;
coinbase_at_height(H) when H < 63669 -> 385 * ?MULTIPLIER;
coinbase_at_height(H) when H < 64299 -> 384 * ?MULTIPLIER;
coinbase_at_height(H) when H < 64930 -> 383 * ?MULTIPLIER;
coinbase_at_height(H) when H < 65563 -> 382 * ?MULTIPLIER;
coinbase_at_height(H) when H < 66196 -> 381 * ?MULTIPLIER;
coinbase_at_height(H) when H < 66832 -> 380 * ?MULTIPLIER;
coinbase_at_height(H) when H < 67469 -> 379 * ?MULTIPLIER;
coinbase_at_height(H) when H < 68107 -> 378 * ?MULTIPLIER;
coinbase_at_height(H) when H < 68747 -> 377 * ?MULTIPLIER;
coinbase_at_height(H) when H < 69388 -> 376 * ?MULTIPLIER;
coinbase_at_height(H) when H < 70031 -> 375 * ?MULTIPLIER;
coinbase_at_height(H) when H < 70675 -> 374 * ?MULTIPLIER;
coinbase_at_height(H) when H < 71321 -> 373 * ?MULTIPLIER;
coinbase_at_height(H) when H < 71969 -> 372 * ?MULTIPLIER;
coinbase_at_height(H) when H < 72619 -> 371 * ?MULTIPLIER;
coinbase_at_height(H) when H < 73270 -> 370 * ?MULTIPLIER;
coinbase_at_height(H) when H < 73923 -> 369 * ?MULTIPLIER;
coinbase_at_height(H) when H < 74578 -> 368 * ?MULTIPLIER;
coinbase_at_height(H) when H < 75234 -> 367 * ?MULTIPLIER;
coinbase_at_height(H) when H < 75893 -> 366 * ?MULTIPLIER;
coinbase_at_height(H) when H < 76553 -> 365 * ?MULTIPLIER;
coinbase_at_height(H) when H < 77215 -> 364 * ?MULTIPLIER;
coinbase_at_height(H) when H < 77879 -> 363 * ?MULTIPLIER;
coinbase_at_height(H) when H < 78546 -> 362 * ?MULTIPLIER;
coinbase_at_height(H) when H < 79214 -> 361 * ?MULTIPLIER;
coinbase_at_height(H) when H < 79884 -> 360 * ?MULTIPLIER;
coinbase_at_height(H) when H < 80556 -> 359 * ?MULTIPLIER;
coinbase_at_height(H) when H < 81230 -> 358 * ?MULTIPLIER;
coinbase_at_height(H) when H < 81907 -> 357 * ?MULTIPLIER;
coinbase_at_height(H) when H < 82585 -> 356 * ?MULTIPLIER;
coinbase_at_height(H) when H < 83266 -> 355 * ?MULTIPLIER;
coinbase_at_height(H) when H < 83949 -> 354 * ?MULTIPLIER;
coinbase_at_height(H) when H < 84634 -> 353 * ?MULTIPLIER;
coinbase_at_height(H) when H < 85321 -> 352 * ?MULTIPLIER;
coinbase_at_height(H) when H < 86011 -> 351 * ?MULTIPLIER;
coinbase_at_height(H) when H < 86703 -> 350 * ?MULTIPLIER;
coinbase_at_height(H) when H < 87398 -> 349 * ?MULTIPLIER;
coinbase_at_height(H) when H < 88094 -> 348 * ?MULTIPLIER;
coinbase_at_height(H) when H < 88794 -> 347 * ?MULTIPLIER;
coinbase_at_height(H) when H < 89495 -> 346 * ?MULTIPLIER;
coinbase_at_height(H) when H < 90199 -> 345 * ?MULTIPLIER;
coinbase_at_height(H) when H < 90906 -> 344 * ?MULTIPLIER;
coinbase_at_height(H) when H < 91615 -> 343 * ?MULTIPLIER;
coinbase_at_height(H) when H < 92327 -> 342 * ?MULTIPLIER;
coinbase_at_height(H) when H < 93042 -> 341 * ?MULTIPLIER;
coinbase_at_height(H) when H < 93759 -> 340 * ?MULTIPLIER;
coinbase_at_height(H) when H < 94478 -> 339 * ?MULTIPLIER;
coinbase_at_height(H) when H < 95201 -> 338 * ?MULTIPLIER;
coinbase_at_height(H) when H < 95926 -> 337 * ?MULTIPLIER;
coinbase_at_height(H) when H < 96654 -> 336 * ?MULTIPLIER;
coinbase_at_height(H) when H < 97384 -> 335 * ?MULTIPLIER;
coinbase_at_height(H) when H < 98118 -> 334 * ?MULTIPLIER;
coinbase_at_height(H) when H < 98854 -> 333 * ?MULTIPLIER;
coinbase_at_height(H) when H < 99594 -> 332 * ?MULTIPLIER;
coinbase_at_height(H) when H < 100336 -> 331 * ?MULTIPLIER;
coinbase_at_height(H) when H < 101081 -> 330 * ?MULTIPLIER;
coinbase_at_height(H) when H < 101829 -> 329 * ?MULTIPLIER;
coinbase_at_height(H) when H < 102581 -> 328 * ?MULTIPLIER;
coinbase_at_height(H) when H < 103335 -> 327 * ?MULTIPLIER;
coinbase_at_height(H) when H < 104092 -> 326 * ?MULTIPLIER;
coinbase_at_height(H) when H < 104853 -> 325 * ?MULTIPLIER;
coinbase_at_height(H) when H < 105617 -> 324 * ?MULTIPLIER;
coinbase_at_height(H) when H < 106384 -> 323 * ?MULTIPLIER;
coinbase_at_height(H) when H < 107154 -> 322 * ?MULTIPLIER;
coinbase_at_height(H) when H < 107927 -> 321 * ?MULTIPLIER;
coinbase_at_height(H) when H < 108704 -> 320 * ?MULTIPLIER;
coinbase_at_height(H) when H < 109484 -> 319 * ?MULTIPLIER;
coinbase_at_height(H) when H < 110268 -> 318 * ?MULTIPLIER;
coinbase_at_height(H) when H < 111055 -> 317 * ?MULTIPLIER;
coinbase_at_height(H) when H < 111845 -> 316 * ?MULTIPLIER;
coinbase_at_height(H) when H < 112639 -> 315 * ?MULTIPLIER;
coinbase_at_height(H) when H < 113437 -> 314 * ?MULTIPLIER;
coinbase_at_height(H) when H < 114238 -> 313 * ?MULTIPLIER;
coinbase_at_height(H) when H < 115043 -> 312 * ?MULTIPLIER;
coinbase_at_height(H) when H < 115851 -> 311 * ?MULTIPLIER;
coinbase_at_height(H) when H < 116663 -> 310 * ?MULTIPLIER;
coinbase_at_height(H) when H < 117479 -> 309 * ?MULTIPLIER;
coinbase_at_height(H) when H < 118299 -> 308 * ?MULTIPLIER;
coinbase_at_height(H) when H < 119122 -> 307 * ?MULTIPLIER;
coinbase_at_height(H) when H < 119950 -> 306 * ?MULTIPLIER;
coinbase_at_height(H) when H < 120781 -> 305 * ?MULTIPLIER;
coinbase_at_height(H) when H < 121616 -> 304 * ?MULTIPLIER;
coinbase_at_height(H) when H < 122455 -> 303 * ?MULTIPLIER;
coinbase_at_height(H) when H < 123299 -> 302 * ?MULTIPLIER;
coinbase_at_height(H) when H < 124146 -> 301 * ?MULTIPLIER;
coinbase_at_height(H) when H < 124997 -> 300 * ?MULTIPLIER;
coinbase_at_height(H) when H < 125853 -> 299 * ?MULTIPLIER;
coinbase_at_height(H) when H < 126713 -> 298 * ?MULTIPLIER;
coinbase_at_height(H) when H < 127577 -> 297 * ?MULTIPLIER;
coinbase_at_height(H) when H < 128446 -> 296 * ?MULTIPLIER;
coinbase_at_height(H) when H < 129319 -> 295 * ?MULTIPLIER;
coinbase_at_height(H) when H < 130196 -> 294 * ?MULTIPLIER;
coinbase_at_height(H) when H < 131077 -> 293 * ?MULTIPLIER;
coinbase_at_height(H) when H < 131964 -> 292 * ?MULTIPLIER;
coinbase_at_height(H) when H < 132855 -> 291 * ?MULTIPLIER;
coinbase_at_height(H) when H < 133750 -> 290 * ?MULTIPLIER;
coinbase_at_height(H) when H < 134650 -> 289 * ?MULTIPLIER;
coinbase_at_height(H) when H < 135555 -> 288 * ?MULTIPLIER;
coinbase_at_height(H) when H < 136464 -> 287 * ?MULTIPLIER;
coinbase_at_height(H) when H < 137379 -> 286 * ?MULTIPLIER;
coinbase_at_height(H) when H < 138298 -> 285 * ?MULTIPLIER;
coinbase_at_height(H) when H < 139222 -> 284 * ?MULTIPLIER;
coinbase_at_height(H) when H < 140151 -> 283 * ?MULTIPLIER;
coinbase_at_height(H) when H < 141085 -> 282 * ?MULTIPLIER;
coinbase_at_height(H) when H < 142024 -> 281 * ?MULTIPLIER;
coinbase_at_height(H) when H < 142969 -> 280 * ?MULTIPLIER;
coinbase_at_height(H) when H < 143918 -> 279 * ?MULTIPLIER;
coinbase_at_height(H) when H < 144873 -> 278 * ?MULTIPLIER;
coinbase_at_height(H) when H < 145833 -> 277 * ?MULTIPLIER;
coinbase_at_height(H) when H < 146799 -> 276 * ?MULTIPLIER;
coinbase_at_height(H) when H < 147769 -> 275 * ?MULTIPLIER;
coinbase_at_height(H) when H < 148746 -> 274 * ?MULTIPLIER;
coinbase_at_height(H) when H < 149728 -> 273 * ?MULTIPLIER;
coinbase_at_height(H) when H < 150715 -> 272 * ?MULTIPLIER;
coinbase_at_height(H) when H < 151709 -> 271 * ?MULTIPLIER;
coinbase_at_height(H) when H < 152708 -> 270 * ?MULTIPLIER;
coinbase_at_height(H) when H < 153712 -> 269 * ?MULTIPLIER;
coinbase_at_height(H) when H < 154723 -> 268 * ?MULTIPLIER;
coinbase_at_height(H) when H < 155740 -> 267 * ?MULTIPLIER;
coinbase_at_height(H) when H < 156762 -> 266 * ?MULTIPLIER;
coinbase_at_height(H) when H < 157791 -> 265 * ?MULTIPLIER;
coinbase_at_height(H) when H < 158826 -> 264 * ?MULTIPLIER;
coinbase_at_height(H) when H < 159867 -> 263 * ?MULTIPLIER;
coinbase_at_height(H) when H < 160914 -> 262 * ?MULTIPLIER;
coinbase_at_height(H) when H < 161968 -> 261 * ?MULTIPLIER;
coinbase_at_height(H) when H < 163028 -> 260 * ?MULTIPLIER;
coinbase_at_height(H) when H < 164095 -> 259 * ?MULTIPLIER;
coinbase_at_height(H) when H < 165168 -> 258 * ?MULTIPLIER;
coinbase_at_height(H) when H < 166248 -> 257 * ?MULTIPLIER;
coinbase_at_height(H) when H < 167334 -> 256 * ?MULTIPLIER;
coinbase_at_height(H) when H < 168428 -> 255 * ?MULTIPLIER;
coinbase_at_height(H) when H < 169528 -> 254 * ?MULTIPLIER;
coinbase_at_height(H) when H < 170635 -> 253 * ?MULTIPLIER;
coinbase_at_height(H) when H < 171749 -> 252 * ?MULTIPLIER;
coinbase_at_height(H) when H < 172871 -> 251 * ?MULTIPLIER;
coinbase_at_height(H) when H < 173999 -> 250 * ?MULTIPLIER;
coinbase_at_height(H) when H < 175135 -> 249 * ?MULTIPLIER;
coinbase_at_height(H) when H < 176279 -> 248 * ?MULTIPLIER;
coinbase_at_height(H) when H < 177430 -> 247 * ?MULTIPLIER;
coinbase_at_height(H) when H < 178588 -> 246 * ?MULTIPLIER;
coinbase_at_height(H) when H < 179754 -> 245 * ?MULTIPLIER;
coinbase_at_height(H) when H < 180928 -> 244 * ?MULTIPLIER;
coinbase_at_height(H) when H < 182110 -> 243 * ?MULTIPLIER;
coinbase_at_height(H) when H < 183299 -> 242 * ?MULTIPLIER;
coinbase_at_height(H) when H < 184497 -> 241 * ?MULTIPLIER;
coinbase_at_height(H) when H < 185703 -> 240 * ?MULTIPLIER;
coinbase_at_height(H) when H < 186917 -> 239 * ?MULTIPLIER;
coinbase_at_height(H) when H < 188139 -> 238 * ?MULTIPLIER;
coinbase_at_height(H) when H < 189370 -> 237 * ?MULTIPLIER;
coinbase_at_height(H) when H < 190609 -> 236 * ?MULTIPLIER;
coinbase_at_height(H) when H < 191857 -> 235 * ?MULTIPLIER;
coinbase_at_height(H) when H < 193114 -> 234 * ?MULTIPLIER;
coinbase_at_height(H) when H < 194380 -> 233 * ?MULTIPLIER;
coinbase_at_height(H) when H < 195654 -> 232 * ?MULTIPLIER;
coinbase_at_height(H) when H < 196938 -> 231 * ?MULTIPLIER;
coinbase_at_height(H) when H < 198231 -> 230 * ?MULTIPLIER;
coinbase_at_height(H) when H < 199533 -> 229 * ?MULTIPLIER;
coinbase_at_height(H) when H < 200844 -> 228 * ?MULTIPLIER;
coinbase_at_height(H) when H < 202166 -> 227 * ?MULTIPLIER;
coinbase_at_height(H) when H < 203497 -> 226 * ?MULTIPLIER;
coinbase_at_height(H) when H < 204837 -> 225 * ?MULTIPLIER;
coinbase_at_height(H) when H < 206188 -> 224 * ?MULTIPLIER;
coinbase_at_height(H) when H < 207549 -> 223 * ?MULTIPLIER;
coinbase_at_height(H) when H < 208919 -> 222 * ?MULTIPLIER;
coinbase_at_height(H) when H < 210301 -> 221 * ?MULTIPLIER;
coinbase_at_height(H) when H < 211692 -> 220 * ?MULTIPLIER;
coinbase_at_height(H) when H < 213095 -> 219 * ?MULTIPLIER;
coinbase_at_height(H) when H < 214508 -> 218 * ?MULTIPLIER;
coinbase_at_height(H) when H < 215931 -> 217 * ?MULTIPLIER;
coinbase_at_height(H) when H < 217366 -> 216 * ?MULTIPLIER;
coinbase_at_height(H) when H < 218812 -> 215 * ?MULTIPLIER;
coinbase_at_height(H) when H < 220270 -> 214 * ?MULTIPLIER;
coinbase_at_height(H) when H < 221739 -> 213 * ?MULTIPLIER;
coinbase_at_height(H) when H < 223219 -> 212 * ?MULTIPLIER;
coinbase_at_height(H) when H < 224711 -> 211 * ?MULTIPLIER;
coinbase_at_height(H) when H < 226216 -> 210 * ?MULTIPLIER;
coinbase_at_height(H) when H < 227732 -> 209 * ?MULTIPLIER;
coinbase_at_height(H) when H < 229260 -> 208 * ?MULTIPLIER;
coinbase_at_height(H) when H < 230802 -> 207 * ?MULTIPLIER;
coinbase_at_height(H) when H < 232355 -> 206 * ?MULTIPLIER;
coinbase_at_height(H) when H < 233922 -> 205 * ?MULTIPLIER;
coinbase_at_height(H) when H < 235501 -> 204 * ?MULTIPLIER;
coinbase_at_height(H) when H < 237094 -> 203 * ?MULTIPLIER;
coinbase_at_height(H) when H < 238700 -> 202 * ?MULTIPLIER;
coinbase_at_height(H) when H < 240319 -> 201 * ?MULTIPLIER;
coinbase_at_height(H) when H < 241952 -> 200 * ?MULTIPLIER;
coinbase_at_height(H) when H < 243600 -> 199 * ?MULTIPLIER;
coinbase_at_height(H) when H < 245261 -> 198 * ?MULTIPLIER;
coinbase_at_height(H) when H < 246937 -> 197 * ?MULTIPLIER;
coinbase_at_height(H) when H < 248627 -> 196 * ?MULTIPLIER;
coinbase_at_height(H) when H < 250332 -> 195 * ?MULTIPLIER;
coinbase_at_height(H) when H < 252051 -> 194 * ?MULTIPLIER;
coinbase_at_height(H) when H < 253786 -> 193 * ?MULTIPLIER;
coinbase_at_height(H) when H < 255537 -> 192 * ?MULTIPLIER;
coinbase_at_height(H) when H < 257303 -> 191 * ?MULTIPLIER;
coinbase_at_height(H) when H < 259085 -> 190 * ?MULTIPLIER;
coinbase_at_height(H) when H < 260883 -> 189 * ?MULTIPLIER;
coinbase_at_height(H) when H < 262698 -> 188 * ?MULTIPLIER;
coinbase_at_height(H) when H < 264529 -> 187 * ?MULTIPLIER;
coinbase_at_height(H) when H < 266377 -> 186 * ?MULTIPLIER;
coinbase_at_height(H) when H < 268242 -> 185 * ?MULTIPLIER;
coinbase_at_height(H) when H < 270124 -> 184 * ?MULTIPLIER;
coinbase_at_height(H) when H < 272024 -> 183 * ?MULTIPLIER;
coinbase_at_height(H) when H < 273942 -> 182 * ?MULTIPLIER;
coinbase_at_height(H) when H < 275878 -> 181 * ?MULTIPLIER;
coinbase_at_height(H) when H < 277833 -> 180 * ?MULTIPLIER;
coinbase_at_height(H) when H < 279806 -> 179 * ?MULTIPLIER;
coinbase_at_height(H) when H < 281799 -> 178 * ?MULTIPLIER;
coinbase_at_height(H) when H < 283811 -> 177 * ?MULTIPLIER;
coinbase_at_height(H) when H < 285843 -> 176 * ?MULTIPLIER;
coinbase_at_height(H) when H < 287894 -> 175 * ?MULTIPLIER;
coinbase_at_height(H) when H < 289967 -> 174 * ?MULTIPLIER;
coinbase_at_height(H) when H < 292059 -> 173 * ?MULTIPLIER;
coinbase_at_height(H) when H < 294173 -> 172 * ?MULTIPLIER;
coinbase_at_height(H) when H < 296308 -> 171 * ?MULTIPLIER;
coinbase_at_height(H) when H < 298465 -> 170 * ?MULTIPLIER;
coinbase_at_height(H) when H < 300644 -> 169 * ?MULTIPLIER;
coinbase_at_height(H) when H < 302845 -> 168 * ?MULTIPLIER;
coinbase_at_height(H) when H < 305070 -> 167 * ?MULTIPLIER;
coinbase_at_height(H) when H < 307317 -> 166 * ?MULTIPLIER;
coinbase_at_height(H) when H < 309588 -> 165 * ?MULTIPLIER;
coinbase_at_height(H) when H < 311884 -> 164 * ?MULTIPLIER;
coinbase_at_height(H) when H < 314203 -> 163 * ?MULTIPLIER;
coinbase_at_height(H) when H < 316548 -> 162 * ?MULTIPLIER;
coinbase_at_height(H) when H < 318918 -> 161 * ?MULTIPLIER;
coinbase_at_height(H) when H < 321313 -> 160 * ?MULTIPLIER;
coinbase_at_height(H) when H < 323735 -> 159 * ?MULTIPLIER;
coinbase_at_height(H) when H < 326183 -> 158 * ?MULTIPLIER;
coinbase_at_height(H) when H < 328659 -> 157 * ?MULTIPLIER;
coinbase_at_height(H) when H < 331162 -> 156 * ?MULTIPLIER;
coinbase_at_height(H) when H < 333694 -> 155 * ?MULTIPLIER;
coinbase_at_height(H) when H < 336254 -> 154 * ?MULTIPLIER;
coinbase_at_height(H) when H < 338844 -> 153 * ?MULTIPLIER;
coinbase_at_height(H) when H < 341463 -> 152 * ?MULTIPLIER;
coinbase_at_height(H) when H < 344112 -> 151 * ?MULTIPLIER;
coinbase_at_height(H) when H < 346792 -> 150 * ?MULTIPLIER;
coinbase_at_height(H) when H < 349504 -> 149 * ?MULTIPLIER;
coinbase_at_height(H) when H < 352248 -> 148 * ?MULTIPLIER;
coinbase_at_height(H) when H < 355025 -> 147 * ?MULTIPLIER;
coinbase_at_height(H) when H < 357834 -> 146 * ?MULTIPLIER;
coinbase_at_height(H) when H < 360678 -> 145 * ?MULTIPLIER;
coinbase_at_height(H) when H < 363556 -> 144 * ?MULTIPLIER;
coinbase_at_height(H) when H < 366470 -> 143 * ?MULTIPLIER;
coinbase_at_height(H) when H < 369420 -> 142 * ?MULTIPLIER;
coinbase_at_height(H) when H < 372406 -> 141 * ?MULTIPLIER;
coinbase_at_height(H) when H < 375430 -> 140 * ?MULTIPLIER;
coinbase_at_height(H) when H < 378492 -> 139 * ?MULTIPLIER;
coinbase_at_height(H) when H < 381593 -> 138 * ?MULTIPLIER;
coinbase_at_height(H) when H < 384734 -> 137 * ?MULTIPLIER;
coinbase_at_height(H) when H < 387915 -> 136 * ?MULTIPLIER;
coinbase_at_height(H) when H < 391138 -> 135 * ?MULTIPLIER;
coinbase_at_height(H) when H < 394403 -> 134 * ?MULTIPLIER;
coinbase_at_height(H) when H < 397712 -> 133 * ?MULTIPLIER;
coinbase_at_height(H) when H < 401064 -> 132 * ?MULTIPLIER;
coinbase_at_height(H) when H < 404462 -> 131 * ?MULTIPLIER;
coinbase_at_height(H) when H < 407906 -> 130 * ?MULTIPLIER;
coinbase_at_height(H) when H < 411397 -> 129 * ?MULTIPLIER;
coinbase_at_height(H) when H < 414936 -> 128 * ?MULTIPLIER;
coinbase_at_height(H) when H < 418524 -> 127 * ?MULTIPLIER;
coinbase_at_height(H) when H < 422162 -> 126 * ?MULTIPLIER;
coinbase_at_height(H) when H < 425852 -> 125 * ?MULTIPLIER;
coinbase_at_height(H) when H < 429594 -> 124 * ?MULTIPLIER;
coinbase_at_height(H) when H < 433390 -> 123 * ?MULTIPLIER;
coinbase_at_height(H) when H < 437242 -> 122 * ?MULTIPLIER;
coinbase_at_height(H) when H < 441149 -> 121 * ?MULTIPLIER;
coinbase_at_height(H) when H < 445114 -> 120 * ?MULTIPLIER;
coinbase_at_height(H) when H < 449138 -> 119 * ?MULTIPLIER;
coinbase_at_height(H) when H < 453222 -> 118 * ?MULTIPLIER;
coinbase_at_height(H) when H < 457368 -> 117 * ?MULTIPLIER;
coinbase_at_height(H) when H < 461578 -> 116 * ?MULTIPLIER;
coinbase_at_height(H) when H < 465852 -> 115 * ?MULTIPLIER;
coinbase_at_height(H) when H < 470192 -> 114 * ?MULTIPLIER;
coinbase_at_height(H) when H < 474601 -> 113 * ?MULTIPLIER;
coinbase_at_height(H) when H < 479080 -> 112 * ?MULTIPLIER;
coinbase_at_height(H) when H < 483630 -> 111 * ?MULTIPLIER;
coinbase_at_height(H) when H < 488253 -> 110 * ?MULTIPLIER;
coinbase_at_height(H) when H < 492952 -> 109 * ?MULTIPLIER;
coinbase_at_height(H) when H < 497728 -> 108 * ?MULTIPLIER;
coinbase_at_height(H) when H < 502584 -> 107 * ?MULTIPLIER;
coinbase_at_height(H) when H < 507520 -> 106 * ?MULTIPLIER;
coinbase_at_height(H) when H < 512540 -> 105 * ?MULTIPLIER;
coinbase_at_height(H) when H < 517646 -> 104 * ?MULTIPLIER;
coinbase_at_height(H) when H < 522840 -> 103 * ?MULTIPLIER;
coinbase_at_height(H) when H < 528125 -> 102 * ?MULTIPLIER;
coinbase_at_height(H) when H < 533503 -> 101 * ?MULTIPLIER;
coinbase_at_height(H) when H < 538976 -> 100 * ?MULTIPLIER;
coinbase_at_height(H) when H < 544548 -> 99 * ?MULTIPLIER;
coinbase_at_height(H) when H < 550220 -> 98 * ?MULTIPLIER;
coinbase_at_height(H) when H < 555997 -> 97 * ?MULTIPLIER;
coinbase_at_height(H) when H < 561881 -> 96 * ?MULTIPLIER;
coinbase_at_height(H) when H < 567876 -> 95 * ?MULTIPLIER;
coinbase_at_height(H) when H < 573984 -> 94 * ?MULTIPLIER;
coinbase_at_height(H) when H < 580209 -> 93 * ?MULTIPLIER;
coinbase_at_height(H) when H < 586554 -> 92 * ?MULTIPLIER;
coinbase_at_height(H) when H < 593024 -> 91 * ?MULTIPLIER;
coinbase_at_height(H) when H < 599622 -> 90 * ?MULTIPLIER;
coinbase_at_height(H) when H < 606352 -> 89 * ?MULTIPLIER;
coinbase_at_height(H) when H < 613218 -> 88 * ?MULTIPLIER;
coinbase_at_height(H) when H < 620226 -> 87 * ?MULTIPLIER;
coinbase_at_height(H) when H < 627378 -> 86 * ?MULTIPLIER;
coinbase_at_height(H) when H < 634681 -> 85 * ?MULTIPLIER;
coinbase_at_height(H) when H < 642139 -> 84 * ?MULTIPLIER;
coinbase_at_height(H) when H < 649757 -> 83 * ?MULTIPLIER;
coinbase_at_height(H) when H < 657541 -> 82 * ?MULTIPLIER;
coinbase_at_height(H) when H < 665496 -> 81 * ?MULTIPLIER;
coinbase_at_height(H) when H < 673630 -> 80 * ?MULTIPLIER;
coinbase_at_height(H) when H < 681947 -> 79 * ?MULTIPLIER;
coinbase_at_height(H) when H < 690454 -> 78 * ?MULTIPLIER;
coinbase_at_height(H) when H < 699159 -> 77 * ?MULTIPLIER;
coinbase_at_height(H) when H < 708068 -> 76 * ?MULTIPLIER;
coinbase_at_height(H) when H < 717190 -> 75 * ?MULTIPLIER;
coinbase_at_height(H) when H < 726531 -> 74 * ?MULTIPLIER;
coinbase_at_height(H) when H < 736102 -> 73 * ?MULTIPLIER;
coinbase_at_height(H) when H < 745910 -> 72 * ?MULTIPLIER;
coinbase_at_height(H) when H < 755965 -> 71 * ?MULTIPLIER;
coinbase_at_height(H) when H < 766276 -> 70 * ?MULTIPLIER;
coinbase_at_height(H) when H < 776855 -> 69 * ?MULTIPLIER;
coinbase_at_height(H) when H < 787712 -> 68 * ?MULTIPLIER;
coinbase_at_height(H) when H < 798858 -> 67 * ?MULTIPLIER;
coinbase_at_height(H) when H < 810307 -> 66 * ?MULTIPLIER;
coinbase_at_height(H) when H < 822070 -> 65 * ?MULTIPLIER;
coinbase_at_height(H) when H < 834162 -> 64 * ?MULTIPLIER;
coinbase_at_height(H) when H < 846597 -> 63 * ?MULTIPLIER;
coinbase_at_height(H) when H < 859391 -> 62 * ?MULTIPLIER;
coinbase_at_height(H) when H < 872559 -> 61 * ?MULTIPLIER;
coinbase_at_height(H) when H < 886121 -> 60 * ?MULTIPLIER;
coinbase_at_height(H) when H < 900093 -> 59 * ?MULTIPLIER;
coinbase_at_height(H) when H < 914496 -> 58 * ?MULTIPLIER;
coinbase_at_height(H) when H < 929350 -> 57 * ?MULTIPLIER;
coinbase_at_height(H) when H < 944679 -> 56 * ?MULTIPLIER;
coinbase_at_height(H) when H < 960506 -> 55 * ?MULTIPLIER;
coinbase_at_height(H) when H < 976857 -> 54 * ?MULTIPLIER;
coinbase_at_height(H) when H < 993760 -> 53 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1011243 -> 52 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1029340 -> 51 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1048083 -> 50 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1067509 -> 49 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1087659 -> 48 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1108574 -> 47 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1130302 -> 46 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1152891 -> 45 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1176397 -> 44 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1200880 -> 43 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1226402 -> 42 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1253035 -> 41 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1280855 -> 40 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1309948 -> 39 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1340405 -> 38 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1372329 -> 37 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1405832 -> 36 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1441040 -> 35 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1478091 -> 34 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1517139 -> 33 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1558356 -> 32 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1601937 -> 31 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1648097 -> 30 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1697082 -> 29 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1749170 -> 28 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1804676 -> 27 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1863963 -> 26 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1927445 -> 25 * ?MULTIPLIER;
coinbase_at_height(H) when H < 1995602 -> 24 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2068990 -> 23 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2148261 -> 22 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2234180 -> 21 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2327654 -> 20 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2429765 -> 19 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2541820 -> 18 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2665405 -> 17 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2802472 -> 16 * ?MULTIPLIER;
coinbase_at_height(H) when H < 2955448 -> 15 * ?MULTIPLIER;
coinbase_at_height(H) when H < 3127395 -> 14 * ?MULTIPLIER;
coinbase_at_height(H) when H < 3322231 -> 13 * ?MULTIPLIER;
coinbase_at_height(H) when H < 3545057 -> 12 * ?MULTIPLIER;
coinbase_at_height(H) when H < 3802648 -> 11 * ?MULTIPLIER;
coinbase_at_height(H) when H < 4104216 -> 10 * ?MULTIPLIER;
coinbase_at_height(H) when H < 4462623 -> 9 * ?MULTIPLIER;
coinbase_at_height(H) when H < 4896425 -> 8 * ?MULTIPLIER;
coinbase_at_height(H) when H < 5433483 -> 7 * ?MULTIPLIER;
coinbase_at_height(H) when H < 6117735 -> 6 * ?MULTIPLIER;
coinbase_at_height(H) when H < 7023041 -> 5 * ?MULTIPLIER;
coinbase_at_height(H) when H < 8284564 -> 4 * ?MULTIPLIER;
coinbase_at_height(H) when H < 10181218 -> 3 * ?MULTIPLIER;
coinbase_at_height(H) when H < 13405331 -> 2 * ?MULTIPLIER;
coinbase_at_height(H) when H < 20351983 -> 1 * ?MULTIPLIER;
coinbase_at_height(_H) -> 0.
