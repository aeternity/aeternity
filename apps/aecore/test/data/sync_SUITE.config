%% -*- erlang-indent-level: 4; indent-tabs-mode: nil -*-
[
 {setup,
  [{'$setup_hooks',
    [{normal, [
               {10, {aecore_sync_SUITE, start_proxy, []}}
              ]}
    ]}
  ]},
 {lager,
  [{error_logger_whitelist, [aec_test_event_handler]}
  ]}
].
