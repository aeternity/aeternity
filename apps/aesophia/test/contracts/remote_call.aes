
contract RemoteCall =

    function call(r : address, x : int) : int =
        let gas   = 10000
        let value = 10
        raw_call(r, "main", gas, value, x)

    // Multi-argument functions not working yet
    function call42(r : address) : int = call(r, 42)

    function withdraw() : int =
        // if(Call.caller == Contract.owner)    // TODO: implement '=='. Also the EVM doesn't have OWNER?
        let amount = Contract.balance
        raw_spend(Call.caller, amount)
        amount
