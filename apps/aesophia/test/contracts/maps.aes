contract Maps =

  function lookup(m : map(string, string), k) =
    m[k]

  function test() =
    let m = Map.from_list([("foo", "1004"), ("bar", "1005")])
    lookup(m, "bar")

  function empty() : map(string, string) = {}

  function singleton(k, v) : map(string, string) = {[k] = v}

