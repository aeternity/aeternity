contract Init_error =

  record state = {value : int}

  private function require(b : bool, err : string) =
    if(!b) abort(err)

  function init(x: int) =
    require(x > 10, "too small")
    {value = x}
